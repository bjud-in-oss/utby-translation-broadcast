# Steg 3b: Domänkontrakt och fraktal dokumentation (TCK-018)

## 1. Komponentkontrakt för App
- `App` är en parameterlös funktionell React-komponent (`export function App(): React.JSX.Element`).
- Konsumerar namngiven export `LiveTranslationWidget` från `./features/live_translation`.
